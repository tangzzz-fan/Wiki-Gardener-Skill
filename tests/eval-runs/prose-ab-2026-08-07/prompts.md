# 固定题目包（脚本化应答）

Vault：`tests/eval-runs/prose-ab-2026-08-07/vault`  
域：`00_系统/domains/ios.md`（T1）；T2/T3 可无专用域，按通用工程写作 + 宪章。  
体量：宪章默认 800–3000 字；本轮目标 **900–1400 字**（正文，不含 frontmatter）。

每题流程：domain-expert 执笔 → 脚本应答（每轮 ≤3 问）→ 确认大纲 → 落 `10_inbox/` → 拷 `drafts/Tn/arm-a.md`。

---

## T1 实操知识笔记（领域硬）

**触发句**

> Vault 在 `tests/eval-runs/prose-ab-2026-08-07/vault`。请用 domain-expert 执笔模式：写一篇关于「CoreBluetooth 回调不要堵在主线程」的知识库笔记。

**脚本应答（按追问轮次固定答复）**

| 轮 | 用户应答 |
|---|---|
| R1 | 经验：上次在 `centralManagerDidUpdateState` 里同步读文件再 startScan，UI 卡了约 2 秒，仪表盘红了。判断：CB 回调里只该投递轻活，重活丢后台队列。边界：不讲具体配网协议字节，只讲线程与回调纪律。 |
| R2 | 受众：三个月后的自己 + 同组 iOS。深度：写到可落地的队列选择与两条红线即可。素材：伪代码即可，iOS 18 / Xcode 16，标版本。 |
| R3（大纲确认） | 同意大纲：标题即结论；节：踩坑现象 → 结论 → 落地写法 → 红线 → 与库内笔记分工。可以动笔。 |

**材料清单（后处理共用；不得增删）**

1. `centralManagerDidUpdateState` 里同步读文件导致 UI 卡约 2 秒  
2. 判断：回调里只投递轻活，重活后台队列  
3. 不写配网协议字节  
4. 适用 iOS 18 / Xcode 16  
5. 红线方向：主线程阻塞等待 CB 回调；生产勿默认开 AllowDuplicates  

**Arm B 指令**

> 按 humanizer 改写下列正文。保留全部事实与判断，不增事实、人名、数字、API。技术中性语气。只输出改写后的完整 Markdown（含原 frontmatter，可微调 title 措辞但不改结论含义）。

**Arm C 指令**

> 按 human-writing **改稿**（非从零创作）。现实稿：只使用下列材料与正文已有事实，禁止补假经历。保留 `[待核验]`。改完清除冒号路标、翻案腔、破折号（代码与 frontmatter 除外）。只输出完整 Markdown。

---

## T2 观点/立场笔记（有态度）

**触发句**

> Vault 同上。domain-expert 执笔：写「为什么笔记要原子化——一篇只讲一件事」。

**脚本应答**

| 轮 | 用户应答 |
|---|---|
| R1 | 经验：我把「BLE 扫描 + 配网状态机 + 错误码表」塞进一篇，三个月后只想改错误码，整篇不敢动，还和另一篇扫描笔记重复。判断：原子化不是为了好看，是为了敢改、敢链、敢丢。边界：不谈具体软件品牌；不写成二选一说教。 |
| R2 | 受众：未来的自己。立场偏非主流一点：宁可短篇多、互链成本高，也不要「大而全」省事。素材：上面那个失败笔记就够，别编第二个故事。 |
| R3 | 同意大纲：结论标题 → 我踩过的反例 → 原子化三条可操作标准 → 和「大而全」的取舍 → 挂载建议。动笔。 |

**材料清单**

1. 曾把扫描 + 状态机 + 错误码塞一篇，改错误码时整篇不敢动  
2. 与另一篇扫描笔记重复  
3. 判断：原子化为了敢改、敢链、敢丢  
4. 宁可短篇多、互链成本高，不要大而全省事  
5. 不谈软件品牌；不写成道德说教  

**Arm B / C 指令**：同 T1（humanizer / human-writing 改稿约束不变）。

---

## T3 偏科普长答（论坛/公众号口吻仍服务知识库）

**触发句**

> Vault 同上。domain-expert 执笔：写给三个月后的自己——「什么时候该把草稿丢进 archive 而不是硬改」。口吻可稍口语，但仍是知识库笔记（标题即结论、可吸附）。

**脚本应答**

| 轮 | 用户应答 |
|---|---|
| R1 | 经验：有一篇「MQTT QoS 选型」初稿写于换库前，后来选型标准变了，我改了两次仍像补丁文物。判断：当前提假设死了，archive + 新写比缝补便宜。边界：不评价别人的库；只给自己用的丢弃标准。 |
| R2 | 受众：三个月后的自己（忙碌版）。给 3 条可检查信号即可。素材：只有上述这一次；不确定「多久算过时」就标 `[待核验]`。 |
| R3 | 同意大纲：结论 → 那次缝补经历 → 三条丢弃信号 → 丢弃后做什么（新写/链旧）→ 什么情况还该硬改。动笔。 |

**材料清单**

1. MQTT QoS 选型初稿写于换库前  
2. 选型标准已变；改两次仍像补丁文物  
3. 判断：前提假设死了 → archive + 新写更便宜  
4. 三条可检查丢弃信号（由执笔从用户判断展开，不得另造第二次经历）  
5. 「多久算过时」标 `[待核验]`  

**Arm B / C 指令**：同 T1。
